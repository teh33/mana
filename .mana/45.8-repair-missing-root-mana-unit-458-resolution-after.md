---
id: '45.8'
title: Repair missing root mana unit 45.8 resolution after successful implementation
slug: repair-missing-root-mana-unit-458-resolution-after
status: open
priority: 1
created_at: '2026-04-09T13:26:51.181068Z'
updated_at: '2026-04-16T09:42:12.986068Z'
notes: |-
  ---
  2026-04-16T09:42:12.986061+00:00
  2026-04-16 cleanup verification: the active root file now exists at `.mana/45.8-repair-missing-root-mana-unit-458-resolution-after.md`. The previous verify gate was stale because it still expected a nonexistent old implementation-path filename. This unit remains open as a real metadata/lookup bug tracker, but its verify should target the actual active file plus successful native lookup.
labels:
- tower
- mana
- metadata
- bug
- follow-up
parent: '45'
verify: cd /Users/asher/tower && test -f .mana/45.8-repair-missing-root-mana-unit-458-resolution-after.md && mana show 45.8 >/dev/null
kind: epic
---

## Task
Repair the root mana graph so unit `45.8` resolves normally again after the successful `mana-core` approval/promotion schema implementation.

## Current state
- Earlier in-session native mana actions could `show` and `claim` `45.8`.
- Later, `mana show 45.8`, `mana notes_append 45.8`, and `mana close 45.8` returned `Unit not found`.
- The active root `.mana/` directory currently does not contain `.mana/45.8-repair-missing-root-mana-unit-458-resolution-after.md`.
- The implementation itself landed and was verified in code, so the graph now needs reconciliation rather than reimplementation.

## Steps
1. Inspect root `.mana/` state, index, and archive state for `45.8`.
2. Determine whether `45.8` was accidentally archived, deleted, or omitted from index materialization.
3. Restore or recreate the active unit record with the existing task description and current verified completion context.
4. Append notes capturing that the implementation already landed in `mana/crates/mana-core/src/unit/types.rs` and passed `cargo check -p mana-core` plus focused approval/promotion tests.
5. Leave the graph in a state where `mana show 45.8` works and the unit can be updated/closed normally.

## Files
- `.mana/` (inspect and modify as needed)
- `.mana/index.yaml` (inspect/update via normal mana flows)
- `.mana/archive/` (inspect)
- `mana/crates/mana-core/src/unit/types.rs` (read-only evidence)

## Acceptance
- `mana show 45.8` resolves successfully.
- The active graph contains a durable record of the already-implemented approval/promotion schema work.
- The reconciliation notes explain that code was already verified before the graph repair.

## Don't
- Don't reimplement the approval/promotion schema.
- Don't move the work into project-local mana.
- Don't silently discard the broken-resolution evidence.

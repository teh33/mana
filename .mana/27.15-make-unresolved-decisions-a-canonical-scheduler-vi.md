---
id: '27.15'
title: Make unresolved decisions a canonical scheduler-visible blocker in run planning
slug: make-unresolved-decisions-a-canonical-scheduler-vi
status: open
priority: 2
created_at: '2026-04-09T08:56:53.851629Z'
updated_at: '2026-04-24T05:37:11.364051Z'
acceptance: |-
  - unresolved decisions become a durable scheduler-visible blocker path instead of only a CLI warning.
  - `mana-cli` / `mana-core` run planning exposes the canonical blocker using the same typed contract defined for autonomy gating.
  - the design explicitly explains how existing `DecisionWarning` behavior in `mana/crates/mana-cli/src/commands/run/mod.rs` maps to the durable blocker model.
  - the work does not yet change mana-pool keep-going behavior; it only makes the blocker canonical and visible to future consumers.
notes: |-
  ---
  2026-04-09T09:00:48.956060+00:00
  2026-04-09 decomposition alignment:
  - This unit should make unresolved decisions flow through the same canonical blocker vocabulary from 27.13.
  - Keep operator warnings/confirmation as presentation only.
  - 27.21 will later project this canonical blocker into scheduler-facing planning/dispatch structures; do not duplicate that projection logic here.

  ---
  2026-04-09T13:46:58.439123+00:00
  Inspected mana-core/unit types and run planning. Canonical typed blocker already exists as AutonomyBlockerCode::UnresolvedDecision, but mana-core::ops::run currently exposes blocked units only as freeform reason strings while mana-cli separately reloads units to synthesize DecisionWarning. Plan: add a typed blocker code to mana-core run blocked units, emit unresolved decisions through that path, and make CLI DecisionWarning rendering consume canonical blocked entries rather than re-deriving policy from raw unit.decisions.

  ---
  2026-04-09T13:48:25.284323+00:00
  Inspected current state. `mana-core::unit::types` already defines `AutonomyBlockerCode::UnresolvedDecision`, but both run planners still expose blocked units mostly as freeform reasons: `mana-core::ops::run::BlockedUnit` only has `reason: String`, and `mana-cli` keeps a separate `collect_decision_warnings` path that reloads units from `decisions`. Plan is to thread the typed blocker plus decision payload through the blocked planning surface, then have `DecisionWarning` render from canonical blocked entries instead of re-deriving policy.

  ---
  2026-04-09T13:51:15.361443+00:00
  2026-04-09 implementation plan after inspecting local CLI planner: use mana-core as the policy source by adding a small shared helper in `mana-core::ops::run` that maps durable `unit.decisions` to `AutonomyBlockerCode::UnresolvedDecision` plus details. Thread that typed blocker onto `mana-core::ops::run::BlockedUnit`. In `mana-cli` local run planning, consume the same helper to classify decision-blocked units into an overrideable autonomy-blocked list instead of reloading units in `collect_decision_warnings`. Keep current confirmation behavior by letting `run_once` optionally re-include those blocked units after operator confirmation, while dry-run renders them as blocked.

  ---
  2026-04-09T13:51:38.626348+00:00
  Follow-on migration sequencing from 27.16: unresolved decisions should be the first CLI-only path folded into the canonical blocker model, because the data is already durable and the current behavior is still a run-time CLI warning/confirmation path in mana-cli. Target outcome is Unit.autonomy blocked by a typed decision-pending blocker rather than interactive confirmation.

  ---
  2026-04-09T13:54:20.529247+00:00
  Migration ordering follow-on from `docs/rebuild/autonomy-gating-type-placement.md`: unresolved decisions should be the first CLI-only path folded into the canonical blocker model. Replace the current `mana run` warning/confirmation-only behavior with `mana-core` evaluation that sets a typed blocker on the unit-scoped autonomy disposition; after that, CLI may still print warnings, but it should print from the typed disposition rather than re-reading raw `unit.decisions` as a special case.

  ---
  2026-04-09T13:57:27.290202+00:00
  Implemented canonical unresolved-decision blocker propagation. mana-core::ops::run::BlockedUnit now carries blocker: Option<AutonomyBlockerCode> plus decisions payload, and compute_ready_queue marks durable unit.decisions as blocker=UnresolvedDecision with reason='unresolved_decision' in non-simulate planning. mana-cli run/plan now threads the same typed blocker onto skipped units in the actual cmd_run planning path; DecisionWarning is now rendered from skipped units tagged with blocker=UnresolvedDecision instead of being the source of truth. Added focused test in mana-core for blocked reason propagation and a mana-cli test for warning derivation from canonical blocker-tagged skipped units. Verify gate passed: rg on unresolved_decision/DecisionWarning/blocked plus cargo check -p mana-core -p mana-cli.

  ---
  2026-04-09T13:58:01.124264+00:00
  Durable architecture note: the live `mana run` execution path does not consume `mana-core::ops::run::RunPlan` directly; it uses the CLI-local planner in `mana/crates/mana-cli/src/commands/run/plan.rs`. To keep unresolved-decision policy canonical in the real operator path, this unit mirrored the canonical blocker shape there: `plan::BlockedUnit` now carries `blocker: Option<AutonomyBlockerCode>` and `decisions: Vec<String>`, and `plan_dispatch` tags unresolved decisions with `UnresolvedDecision`. `DecisionWarning` in `run/mod.rs` now renders from `plan.skipped` blocker metadata instead of re-deriving from raw `unit.decisions` as policy.

  ---
  2026-04-09T13:58:54.078671+00:00
  Implemented canonical unresolved-decision blocker threading. `mana-core::ops::run::BlockedUnit` now carries typed blocker metadata plus durable decision payload, and `compute_ready_queue` emits `AutonomyBlockerCode::UnresolvedDecision` with reason `unresolved_decision` from `Unit.decisions`. `mana-cli` run planning now renders `DecisionWarning` from canonical blocked planner entries via `collect_decision_warnings_from_skipped_units`, and `run_once` re-plans through `plan_dispatch_with_decision_override(..., allow_unresolved_decisions=true)` only after operator confirmation so CLI is presentation/override, not source of truth. Added focused `mana-core` test proving unresolved decisions become blocked with typed blocker + decisions payload, and updated CLI tests around canonical warning extraction. Verify gate passes: `rg ... unresolved_decision|DecisionWarning|blocked` and `cargo check -p mana-core -p mana-cli`. Note: broader `cargo test -p mana-cli` remains blocked by unrelated pre-existing test compile debt from `AttemptRecord`/`RunRecord` `autonomy_observation` field additions in other command test modules.

  ---
  2026-04-09T13:58:59.653107+00:00
  2026-04-09 implementation complete. Added `mana-core::ops::run::blocked_unit_for_unresolved_decisions` so durable `unit.decisions` map to canonical `AutonomyBlockerCode::UnresolvedDecision` plus decision details in run blocked entries. `compute_ready_queue` now reports unresolved decisions as blocked with typed blocker metadata by default. In `mana-cli` local run planning, `plan::BlockedUnit` now carries `reason`, optional typed `blocker`, and `decisions`; `plan_dispatch` blocks unresolved decisions by default and `plan_dispatch_with_decision_override(..., allow_unresolved_decisions=true)` re-includes them only after operator confirmation. `DecisionWarning` rendering now consumes blocked planner entries instead of re-deriving policy from unit files. Added focused `mana-core` run test `unresolved_decisions_become_canonical_blocked_reason` and updated `mana-cli` run tests to assert warning extraction from canonical skipped blockers. Verify passed: `cd /Users/asher/tower && rg -q 'unresolved_decision|DecisionWarning|blocked' mana/crates/mana-core/src/ops/run.rs mana/crates/mana-cli/src/commands/run/mod.rs && cargo check -p mana-core -p mana-cli`. Note: broader `mana-cli` test compilation still has unrelated existing `autonomy_observation` fixture drift outside this unit's scope.

  ---
  2026-04-09T13:59:20.439590+00:00
  Durable decomposition/result captured for 27.15:
  1. `mana-core::ops::run` owns the canonical unresolved-decision blocker mapping from `Unit.decisions` -> `AutonomyBlockerCode::UnresolvedDecision` + reason `unresolved_decision` + copied decisions payload.
  2. `mana-cli::commands::run::plan` consumes that canonical mapping during planning and exposes it on skipped/blocked planner entries.
  3. `mana-cli::commands::run::mod` renders `DecisionWarning` from blocked planner entries and, after operator confirmation, re-plans with an explicit decision override so the warning remains presentation rather than policy truth.
  4. This stops short of pool-side keep-going changes; 27.21 can later project the same canonical blocker into scheduler-facing planning/dispatch surfaces without duplicating CLI-specific logic.
  5. Verification status: task verify gate passed (`cargo check -p mana-core -p mana-cli`). Separate repo-wide `mana-cli` test compilation is still blocked by unrelated missing `autonomy_observation` fields in other command test modules.
labels:
- architecture
- mana
- cli
- dispatch
- autonomy
parent: '27'
verify: cd /Users/asher/mana && rg -q 'unresolved_decision|DecisionWarning|blocked' crates/mana-core/src/ops/run.rs crates/mana-cli/src/commands/run/mod.rs && cargo check -p mana-core -p mana-cli
checkpoint: ee1ca89ccbbf0954c7991fa67f1bfc2b95d71e3d
verify_hash: '8a18f72179dc4cb6eda55179a12eeb83f70d5e6ad9943549a46350fa84e4a2b0'
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T13:46:02.752952Z'
  finished_at: '2026-04-24T05:37:11.364051Z'
decisions:
- 'Adopt canonical unresolved-decision blocking at the run-planning seam. `mana-core::ops::run` is the source of truth for mapping durable `Unit.decisions` into blocker code `unresolved_decision` plus preserved decision payload on blocked planner entries. `mana-cli` warning/confirmation remains operator presentation only: it renders from those canonical blocked entries and, if the operator confirms, performs an explicit re-plan override rather than re-deriving policy from raw units. This keeps the blocker scheduler-visible and durable now, while deferring any pool-side consumption changes to later units such as 27.21.'
- 'Resolved implementation policy from 27.15: default run planning must treat non-empty durable `unit.decisions` as the canonical `AutonomyBlockerCode::UnresolvedDecision` blocker and expose the copied decision text on blocked planner entries. Existing CLI warning/confirmation behavior remains only as presentation and override mechanics: the CLI renders from canonical blocked state, then re-plans explicitly with unresolved decisions allowed after operator confirmation rather than remaining the source of truth for blocker policy.'
---

Goal: replace the current bespoke unresolved-decision warning path with a canonical durable blocker representation that future schedulers and UIs can consume.

Current state:
- `mana/crates/mana-cli/src/commands/run/mod.rs` collects unresolved decisions and prints/asks for confirmation via `DecisionWarning`.
- `mana/crates/mana-core/src/unit/mod.rs` already has durable `decisions: Vec<String>`.
- 27.10 concluded that unresolved decisions must become an explicit scheduler-visible blocker code rather than a CLI-only warning path.

Task:
Thread unresolved decisions into the canonical autonomy/blocker contract so the CLI warning becomes a rendering of durable state instead of bespoke policy logic.

Concrete steps:
1. Inspect `mana/crates/mana-cli/src/commands/run/mod.rs` around `DecisionWarning`, `collect_decision_warnings`, and `confirm_dispatch_with_decisions`.
2. Inspect `mana/crates/mana-core/src/ops/run.rs` and identify the right place to expose a canonical blocked reason for unresolved decisions.
3. Refactor the planning/blocker path so unresolved decisions map to the typed blocker code (`unresolved_decision` or equivalent) from the autonomy contract.
4. Keep existing operator-facing warning/confirmation behavior if needed, but make it a presentation of the canonical blocker.
5. Add focused tests for the blocked reason propagation.

Files:
- `mana/crates/mana-cli/src/commands/run/mod.rs` (modify)
- `mana/crates/mana-core/src/ops/run.rs` (modify)
- any nearby tests for run planning / CLI run behavior (modify)

Do not:
- Do not add pool-side keep-going behavior yet.
- Do not make the CLI the source of truth for unresolved-decision policy.
- Do not infer blockers from prose other than the existing durable `decisions` field.

Context from 27.10:
- unresolved decisions must be a durable scheduler-visible blocker.
- the pool should later consume only the explicit blocker/disposition, not bespoke CLI warnings.

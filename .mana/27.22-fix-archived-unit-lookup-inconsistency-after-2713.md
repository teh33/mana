---
id: '27.22'
title: Fix archived unit lookup inconsistency after 27.13 close
slug: fix-archived-unit-lookup-inconsistency-after-2713
status: open
priority: 2
created_at: '2026-04-09T13:50:42.534604Z'
updated_at: '2026-04-16T11:49:50.188574Z'
acceptance: |-
  - `mana show 27.13` resolves the archived unit after close in the root tower mana tree.
  - Focused tests cover archived unit lookup/resolution behavior.
  - No unrelated schema or dispatch behavior changes are introduced.
notes: |-
  ---
  2026-04-09T13:54:40.219038+00:00
  Observed during plan externalization: native `mana show/update` lookups can fail after a unit is archived, which prevented appending fresh notes to closed unit 27.13 even though the archived markdown exists. This follow-up lookup bug matters for durable planning because it makes post-close graph enrichment unreliable unless archived-unit resolution is fixed.

  ---
  2026-04-16T11:49:50.188568+00:00
  2026-04-16 cleanup verification: this unit remains legitimately open. Native `mana show 27.13` still fails after close, so the archived-unit lookup inconsistency persists for this lineage. Leave open as an unresolved metadata bug rather than closing it as drift.
labels:
- mana
- archive
- lookup
- bug
- follow-up
parent: '27'
verify: cd /Users/asher/tower && mana show 27.13 >/dev/null && cargo test -p mana-core ops::show -- --nocapture && cargo test -p mana-cli show -- --nocapture
kind: job
---

Current state:
- Root mana unit `27.13` is archived on disk at `.mana/archive/2026/04/27.13-add-canonical-autonomy-disposition-and-blocker-voc.md` with `status: closed`.
- Native lookup is inconsistent: `mana show 27.13` currently returns `Unit not found: 27.13` even though the archived file exists and earlier list/status surfaces referenced it.
- This caused close/reporting friction while finishing the autonomy-schema slice.

Task:
Diagnose and fix archived unit resolution so `mana show <id>` and related native lookup/update paths can resolve closed archived units consistently after archive.

Concrete steps:
1. Inspect the archived-resolution path used by `mana show` / native lookup in mana-core and mana-cli.
2. Reproduce with archived unit `27.13` in the root `.mana/` tree.
3. Fix the lookup/index/archive resolution bug without changing unrelated dispatch behavior.
4. Add or update focused tests covering archived lookup after close.

Files:
- `mana/crates/mana-core/src/ops/show.rs` (inspect/modify if relevant)
- `mana/crates/mana-core/src/discovery.rs` (inspect/modify if relevant)
- `mana/crates/mana-cli/src/commands/show.rs` or nearby caller path (inspect if relevant)
- nearby tests in the touched files (modify)

Do not:
- Do not broaden this into general archive refactors.
- Do not change unit schema or dispatch planning.
- Do not rely on list/status output as a substitute for direct lookup correctness.

Context:
- During completion of 27.13, `mana show 27.13` failed while the archived file existed at `.mana/archive/2026/04/27.13-add-canonical-autonomy-disposition-and-blocker-voc.md`.
- The fix should make direct lookup of archived units reliable enough for follow-up close/show/update flows.

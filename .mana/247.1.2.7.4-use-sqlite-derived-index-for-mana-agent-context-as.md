---
id: '247.1.2.7.4'
title: Use SQLite derived index for mana agent context assembly
slug: use-sqlite-derived-index-for-mana-agent-context-as
status: open
priority: 1
created_at: '2026-04-27T18:27:35.037540Z'
updated_at: '2026-04-27T21:46:11.226784Z'
acceptance: Agent context assembly uses the stable mana SQLite query API when the index is fresh, rebuilds or falls back when stale/missing, blocks invalid relevant units, and has targeted tests proving the behavior.
notes: |
  ---
  2026-04-27T19:22:44.670232+00:00
  Before implementation, read parent unit 247.1.2.7 notes. The canonical design source is now embedded in mana there. Context assembly should use SQLite only through stable mana-core query APIs when fresh/valid, and block invalid relevant units.
  ## Attempt 1 — 2026-04-27T21:46:11Z
  Exit code: 1

  ```
  error: unexpected argument 'sqlite' found

  Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

  For more information, try '--help'.
  ```
labels:
- mana
- sqlite
- context
- agent
- implementation
parent: '247.1.2.7'
dependencies:
- '247.1.2.7.2'
- '247.1.2.7.3'
verify: cd /Users/asher/mana && cargo test -p mana-core context sqlite -- --nocapture && rg -q 'context.*sqlite|sqlite.*context|assemble.*context' crates/mana-core/src crates/mana-cli/src
attempts: 1
history:
- attempt: 1
  started_at: '2026-04-27T21:46:11.172361Z'
  finished_at: '2026-04-27T21:46:11.226781Z'
  duration_secs: 0.054
  result: fail
  exit_code: 1
  output_snippet: |-
    error: unexpected argument 'sqlite' found

    Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

    For more information, try '--help'.
kind: job
---

Goal: Wire agent context assembly/read-heavy mana APIs to the SQLite derived index.

Current state:
- SQLite derived index schema/rebuild/write updates exist.
- Canonical files remain Markdown+YAML.
- User specifically wants SQLite to unlock agent context assembly first.

Steps:
1. Inspect current context assembly/memory-context APIs and identify the stable boundary agents/imp consume.
2. Add/extend a stable mana-core query API for context assembly over SQLite: units, relevant facts, paths, labels, blockers/decisions, attempts/history, dependencies, and evidence-like fields as available.
3. Use SQLite only when freshness checks pass; if stale/missing, rebuild or fall back to file scan per the architecture doc.
4. If invalid units are relevant to requested context, block/fail with precise diagnostics; if unrelated, proceed with valid units and surface diagnostics.
5. Add targeted tests for fresh index query, stale rebuild/fallback, invalid relevant-unit blocking, and no raw SQLite coupling in agent-facing APIs.

Files:
- `mana/crates/mana-core/src/**` context/index/query modules (modify)
- `mana/crates/mana-cli/src/commands/memory_context.rs` or equivalent if CLI path consumes context assembly (modify as needed)
- `imp/crates/imp-core` only if stable API wiring requires consumer changes (avoid if possible in this unit)

In scope:
- Context assembly read path.

Out of scope:
- Generic list/search/dependency graph migration.
- Raw SQLite access from agents.

Do not:
- Let agents query SQLite directly.
- Trust stale SQLite over canonical files.
- Silently omit invalid relevant units.

---
id: '247.1.2.7.5'
title: Add mana doctor and index rebuild surfaces for SQLite-derived index
slug: add-mana-doctor-and-index-rebuild-surfaces-for-sql
status: open
priority: 1
created_at: '2026-04-27T18:27:47.595969Z'
updated_at: '2026-04-27T19:22:51.913292Z'
acceptance: CLI/user-facing diagnostics exist for validating canonical mana files and rebuilding the SQLite derived index, including exact file/field errors, stale-index reporting, and tests for invalid-file blocking behavior.
notes: |-
  ---
  2026-04-27T19:22:51.913289+00:00
  Before implementation, read parent unit 247.1.2.7 notes. The canonical design source is now embedded in mana there. Extend existing `mana doctor`; do not create a parallel doctor surface. Doctor fix may rebuild derived indexes but must not silently rewrite canonical files.
labels:
- mana
- doctor
- validation
- sqlite
- cli
parent: '247.1.2.7'
dependencies:
- '247.1.2.7.2'
- '247.1.2.6'
verify: cd /Users/asher/mana && cargo test -p mana-cli doctor index validate -- --nocapture && rg -q 'index rebuild|doctor|validate' crates/mana-cli/src
kind: task
---

Goal: Add validation/doctor/index rebuild user surfaces for human-editable canonical mana files and derived SQLite index health.

Current state:
- Human editing remains first-class.
- Invalid typed files block agent/scheduler use.
- SQLite is derived/rebuildable and must not hide canonical errors.

Steps:
1. Follow the validation design from 247.1.2.6.
2. Add or extend `mana doctor` / `mana validate` / `mana index rebuild` surfaces according to existing CLI conventions.
3. Surface exact diagnostics for invalid canonical files: path, field if available, parse/schema/referential error, and suggested safe next action.
4. Surface stale/missing/corrupt SQLite index state and provide rebuild command/action.
5. Add tests for invalid file diagnostics, stale index reporting, rebuild success, and blocking invalid units from agent/scheduler use.

Files:
- `mana/crates/mana-cli/src/**` (modify — commands/surfaces)
- `mana/crates/mana-core/src/**` (modify — validation/index APIs as needed)

In scope:
- Human-facing validation and rebuild UX.

Out of scope:
- TUI/imp UI polish.
- Auto-fixing files unless explicitly implemented as a safe optional flag after design approval.

Do not:
- Silently repair human files.
- Hide invalid file errors behind SQLite.
- Make warnings non-blocking for agent/scheduler-critical typed breakage.

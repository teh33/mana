---
id: '264.3.1'
title: Add shared imp-core storage path module for canonical ~/.imp and project .imp roots
slug: add-shared-imp-core-storage-path-module-for-canoni
status: open
priority: 1
created_at: '2026-04-16T10:20:34.084034Z'
updated_at: '2026-04-24T05:37:22.566551Z'
notes: |-
  ---
  2026-04-16T10:31:30.974068+00:00
  Implemented the first storage-path slice in code.

  Completed in workspace:
  - Added `imp/crates/imp-core/src/storage.rs` with canonical imp-native root helpers for:
    - global root `~/.imp/`
    - project root `<project>/.imp/`
    - canonical global files/dirs (`config.toml`, `auth.json`, `soul.md`, `agents.md`, `memory.md`, `user.md`, `sessions/`, `indexes/session_index.db`, `skills/`, `prompts/`, `tools/`, `lua/`, `imports/`)
    - canonical project-local files/dirs (`config.toml`, `soul.md`, `agents.md`, `skills/`, `prompts/`, `tools/`, `lua/`)
    - legacy compatibility source roots for XDG config/data and macOS Application Support
  - Exported the module from `imp-core/src/lib.rs`.
  - Switched `Config::{user_config_dir,session_dir,user_config_path}` to delegate to the shared storage helpers so canonical defaults now point at `~/.imp/`.
  - Kept resource-discovery semantics mostly stable while only using storage helpers for project `.imp/*` joins, to avoid mixing the `agents.md` migration into this first path-helper slice.

  Verification so far:
  - `cd /Users/asher/tower && cargo check -p imp-core` passes.
  - `cd /Users/asher/tower && cargo test -p imp-core resources -- --nocapture` passes after keeping user-dir resource semantics stable.
  - `cargo test -p imp-core config -- --nocapture` still shows a pre-existing unrelated failure in `config::tests::agent_mode_planner_blocks_mana_close` asserting planner should not allow mana `update`, even though current `AgentMode::Planner` intentionally allows `update`. I did not change planner mana-action policy in this work.

  Durable sequencing decision:
  - keep canonical `.imp/agents.md` discovery changes for dedicated unit `264.3.3` rather than folding them into the shared path-helper slice.
  - continue auth/session/session-search caller migration in `264.3.2` on top of this storage module.
labels:
- imp
- storage
- implementation
parent: '264.3'
verify: cd /Users/asher/tower && cargo check -p imp-core
checkpoint: '3bc1c847bdc92396c3cee7474c95231937f3178a'
verify_hash: '23de5ed62a242cfbd3fdfc21c23133d64c92410b75634d0a408a25225e09e936'
kind: job
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-16T10:23:17.007253Z'
  finished_at: '2026-04-24T05:37:22.566551Z'
---

Goal: add one shared imp-core storage-path module that defines the canonical imp-native roots and exposes named helpers for all first-class durable imp paths, while still supporting legacy compatibility lookups.

Accepted storage contract:
- global canonical root: `~/.imp/`
- project canonical root: `<project>/.imp/`
- canonical instruction file: `.imp/agents.md`
- XDG/macOS Application Support roots remain compatibility + migration inputs only during rollout

Steps:
1. Introduce a dedicated storage-path module in `imp-core` (either new module or a focused extraction from `config.rs`) that exposes helpers for:
   - global root
   - project root
   - global config/auth/soul/agents/memory/user paths
   - global sessions/indexes/skills/prompts/tools/lua/imports paths
   - project config/soul/agents/skills/prompts/tools/lua paths
   - legacy compatibility source roots
2. Keep the API naming explicit enough that callsites stop hand-rolling joins.
3. Add focused tests for path generation and canonical-vs-legacy behavior on macOS/Linux assumptions.
4. Do not migrate all callers yet in this job unless a tiny number are needed to prove the API is viable.

Files:
- `imp/crates/imp-core/src/config.rs`
- new storage-path module nearby in `imp/crates/imp-core/src/`
- related unit tests

In scope:
- shared path API
- canonical root definitions
- legacy compatibility source helpers

Out of scope:
- broad caller migration
- docs updates
- destructive migration

Do not:
- hard-delete current helper functions until downstream callers move
- strand existing installs that still rely on XDG/Application Support paths

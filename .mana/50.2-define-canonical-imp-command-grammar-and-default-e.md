---
id: '50.2'
title: Define canonical imp command grammar and default-entrypoint migration
slug: define-canonical-imp-command-grammar-and-default-e
status: closed
priority: 1
created_at: '2026-04-09T05:56:49.009337Z'
updated_at: '2026-04-24T05:37:29.357981Z'
notes: |-
  ---
  2026-04-09T06:06:18.322072+00:00
  Command-grammar / product-surface context from user:
  - at minimum, make `imp` much stronger as a CLI
  - evaluate a CLI-first interactive path as the primary authoring/execution UX
  - consider whether a separate viewer/log-navigation surface should exist (`imp view`, `imp logs`, or even a separate binary name) instead of overloading the main interactive surface
  - compare whether plain `imp` should become a line-oriented interactive shell while fullscreen/TUI behavior becomes explicit (`imp tui`) or reduced to a simpler widgetized prompt surface
  - include the possibility that the sidebar/tool-inspection affordance remains valuable, but not necessarily in the main default interaction mode

  ---
  2026-04-09T06:08:20.437808+00:00
  Command-grammar implications from latest discussion:
  - evaluate target command family around three surfaces:
    - `imp` / `imp chat` for CLI-first interactive shell
    - `imp run <unit>` for worker/runtime
    - `imp view` for inspection/log/session browsing
  - `imp tui` should be considered an explicit fullscreen entrypoint rather than assumed default
  - compare whether `imp` should eventually equal `imp chat`, or whether `imp chat` exists first as a compatibility phase before flipping the default
  - avoid separate binary branding like `imp2`; prefer explicit subcommands/entrypoints inside the same tool
  - the command-grammar doc should evaluate both slash/colon in-shell commands and what minimum inline shell affordances replace current fullscreen controls

  ---
  2026-04-09T07:19:54.436310+00:00
  Loaded mana skill per user instruction, claimed unit 50.2, and inspected current baseline sources. Initial evidence: imp/README.md still presents plain `imp` as the default interactive entrypoint, `imp -p` as one-shot print mode, `imp run <unit-id>` as the headless mana-worker path, and login/secrets flows as separate top-level commands. Also confirmed the expected target framing from unit notes: evaluate `imp`/`imp chat` as the CLI-first shell, `imp run` as the machine/worker runtime, `imp view` as the browsing/inspection companion surface, and `imp tui` as an explicit fullscreen entrypoint rather than an assumed default. Next step is to inspect current CLI help/usage strings in imp-cli and then write docs/rebuild/imp-command-grammar.md with the default-entrypoint migration decision centered rather than deferred.

  ---
  2026-04-09T07:20:23.330506+00:00
  Loaded mana skill and root docs. Inspecting current imp README/main.rs plus adjacent feature-50 rebuild specs to ground a command-grammar recommendation with the default-entrypoint decision as the center.

  ---
  2026-04-09T07:22:21.948767+00:00
  Durable delta from 50.1 spec: command-grammar/default-entrypoint work must preserve an explicit unresolved product decision rather than smuggling it into implementation. The question remains: after a CLI-first interactive shell exists, should plain `imp` stay the default interactive entrypoint, or should explicit entrypoints such as `imp tui` and/or `imp chat` carry that meaning? Spec guidance: do not block the first architectural slice on this choice, but keep it visible because it affects docs, rollout, migration cost, and user expectations.

  ---
  2026-04-09T07:24:19.364175+00:00
  Wrote docs/rebuild/imp-command-grammar.md and verified the unit gate. The doc is grounded in current repo reality from imp/crates/imp-cli/src/main.rs, imp/README.md, and docs/rebuild/imp-run-primary-execution-path.md. It makes the default-entrypoint decision explicit rather than deferring it: recommended target is plain `imp` == `imp chat`, with `imp tui` becoming the explicit fullscreen entrypoint, `imp prompt` becoming the canonical name for current `imp -p`, `imp run <unit-id>` staying the canonical machine/mana-worker path, and `imp view` grouping browsing/inspection flows. It also specifies transition phases, compatibility aliases (`-p`, slash commands, `web-login`), and exact stdin/tty default-behavior rules so the migration is not hand-wavy.

  ---
  2026-04-09T07:50:10.407777+00:00
  Durable delta externalized per user request after verify pass:
  - This unit's outcome is now the canonical grammar recommendation doc for the CLI-first imp surface.
  - Explicit command-family recommendation preserved here for downstream implementation/review:
    - `imp chat` = human-facing interactive shell
    - `imp prompt` = canonical one-shot prompt/print path; keep `-p` / `--print` as compatibility aliases
    - `imp run <unit-id>` = machine-facing / mana-worker runtime contract
    - `imp view` = browsing-heavy inspection family
    - `imp tui` = explicit fullscreen adapter
  - Default-entrypoint migration recommendation preserved here:
    - long-term target is plain `imp` == `imp chat`
    - during transition, ship explicit `chat`/`tui`/`view`/`prompt` first and keep plain `imp` temporarily compatible
    - do not introduce a hidden chooser beyond normal tty/stdin behavior
  - Exact default rule to preserve in implementation planning:
    - tty + no subcommand => `imp chat`
    - piped stdin + no subcommand => one-shot prompt behavior equivalent to `imp prompt`
  - Follow-on implication: future implementation units should treat the shell/view/tui split as an execution contract, not reopen the grammar from scratch unless product direction changes.

  ---
  2026-04-09T07:55:37.935369+00:00
  Command-grammar follow-up from the taxonomy spec: preserve the long-term grammar `imp`, `imp chat`, `imp prompt`, `imp run`, `imp view`, `imp tui`, but treat the transition explicitly. Plain `imp` stays compatibility-first until shell parity is good enough, then flips to `imp chat`. Direct CLI families should align with the taxonomy names rather than ad hoc one-offs.

  ---
  2026-04-15T06:59:41.607390+00:00
  Command-grammar follow-up from the latest architecture clarification:
  - Active Tower codebase confirms durable scheduling/orchestration belongs to mana itself (`mana run` / `mana-pool` / `mana-core::ops::run`), while imp owns the single-worker runtime and native UX for invoking mana.
  - That suggests a cleaner command split than the old `imp run` wording:
    - `imp mana <id>` for single-unit worker execution (imp runtime working one mana unit)
    - a separate `imp mana` reserved orchestration verb for multi-agent scheduling/management/watching (backed by mana-owned scheduling, not an imp-local scheduler)
  - This should now be treated as the leading CLI grammar direction before further work on the runtime-connection refactor. Any implementation of 28.1.5.3.1 should be revised to fit this root command-grammar direction rather than optimizing the temporary `imp work` surface further.

  ---
  2026-04-15T07:20:52.763565+00:00
  Implementation progress checkpoint for the emerging `imp mana` command grammar:
  - The current imp CLI parser has been pivoted from the temporary `imp work` experiment toward an explicit `imp mana` namespace.
  - The concrete parser shape in progress is:
    - `imp mana <unit-id>` -> canonical single-unit worker entrypoint
    - `imp run <unit-id>` -> hidden compatibility alias during migration
    - reserved `imp mana status/show/logs/run` tokens held for future mana-aware operator verbs
  - This continues to treat the CLI grammar as an operator-facing surface only; native agent execution is still expected to use embedded mana/imp tool seams rather than bashing the CLI.

  Open implementation step still in flight: add the initial reserved-verb handler/help behavior and finish docs/tests so the namespace is coherent before broader runtime-selection refactors continue.

  ---
  2026-04-24T05:37:29.357978+00:00
  Graph hygiene pass 7 2026-04-24: verify passed for canonical command grammar/default-entrypoint migration doc. Closing stale in_progress planning unit.
labels:
- architecture
- imp
- cli
- ux
- commands
parent: '50'
verify: cd /Users/asher/tower && test -f docs/rebuild/imp-command-grammar.md && rg -q 'imp tui' docs/rebuild/imp-command-grammar.md && rg -q 'imp run' docs/rebuild/imp-command-grammar.md && rg -q 'imp chat' docs/rebuild/imp-command-grammar.md && rg -q 'compatibility' docs/rebuild/imp-command-grammar.md
checkpoint: b2376c3dfc450af36974a86b0698ef76f86d824b
verify_hash: '0afbefd46ae0f51886cc7a2036a26f56d036dea6a32e81113edaa7ed094cf0f9'
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T07:19:40.946998Z'
  finished_at: '2026-04-24T05:37:22.549807Z'
decisions:
- Recording the resolved migration decision from `docs/rebuild/imp-default-entrypoint-migration.md` on the live grammar unit because unit `50.6.5` is not currently addressable through native mana resolution. This keeps the default-entrypoint policy durable on a root unit that downstream work already reads.
- Decision grounded by `docs/rebuild/imp-default-entrypoint-migration.md` plus the prior command-taxonomy and transcript-UX specs. This makes the default-entrypoint policy durable on the command-grammar unit itself rather than only on child 50.6.5.
- |-
  New command-grammar direction from the current discussion: split single-unit mana-worker execution from multi-unit orchestration explicitly under an `imp mana` namespace. Candidate shape:
  - `imp mana <id>` = run one imp worker against one mana unit
  - `imp mana run <target>` (or equivalent reserved orchestration verb) = invoke multi-unit mana scheduling/orchestration over ready jobs / epic scope
  - `imp mana status/show/logs` = mana-aware inspection surfaces
  This keeps single-unit worker entry distinct from orchestration while preserving mana as the scheduling owner.
- |-
  Command-grammar decision update from the latest discussion: the `imp mana` namespace should cover both the direct single-unit worker path and mana-backed orchestration, but with an explicit split of responsibility. Current preferred shape:
  - `imp mana <id>` = imp runtime executes one worker on one mana unit
  - `imp mana <orchestration-verb> ...` = orchestration/inspection entrypoints such as `run`, `status`, `show`, `logs`
  This keeps the namespace coherent while avoiding overloading the single-unit path with scheduling semantics.
- 'CLI/runtime boundary clarification from the latest discussion: the `imp` command grammar is primarily an operator-facing shell/UX surface, not the mechanism agents should use for day-to-day execution. For 99% of agent work, native mana/imp tools should expose the needed single-unit and orchestration capabilities directly so agents do not need to invoke `imp ...` through bash except for compatibility or debugging cases.'
- 'Agent-facing execution principle from the latest discussion: ordinary agent work should not shell out to `imp` via bash. The common path should be native tool/runtime surfaces (`mana` for durable substrate/orchestration, `imp` for child-worker invocation) so mana-backed behavior feels built-in rather than CLI-mediated. CLI grammar under `imp mana ...` is primarily an operator-facing shell surface and compatibility layer, not the primary internal mechanism agents use for 99% of tasks.'
---

Define the canonical command grammar for a CLI-first imp. Write `docs/rebuild/imp-command-grammar.md`. Use current commands and docs as the baseline (`imp`, `imp -p`, `imp run`, login/secrets/config/usage) and propose the target structure: e.g. whether to keep `-p` or add `imp prompt`, whether to add `imp chat`, whether `imp tui` should become explicit, and how human-facing interactive, machine-facing, and mana-worker flows should differ. Include migration recommendations for compatibility, aliases, deprecation timing, and what plain `imp` should do during transition. Do not hand-wave around the default behavior question; make it the center of the doc.

---
id: '27.21'
title: Project autonomy disposition onto ready/planned scheduler surfaces
slug: project-autonomy-disposition-onto-readyplanned-sch
status: open
priority: 2
created_at: '2026-04-09T08:59:41.100008Z'
updated_at: '2026-04-09T14:56:16.665982Z'
acceptance: |-
  - scheduler-facing projection fields for autonomy gating are added to the run-planning / dispatch surface without forcing consumers to inspect raw unit internals.
  - `ReadyUnit` and/or pool-facing `DispatchUnit` receives only explicit typed autonomy disposition/blocker data, not raw confidence and not free-text heuristics.
  - The projection preserves mana-pool’s mechanical role: consume explicit disposition only.
  - No keep-going dispatch behavior change is made in this slice; this is shape/projection only.
  - Tests cover the presence/omission of projected autonomy fields for representative blocked/eligible states.
notes: |-
  ---
  2026-04-09T13:25:55.952943+00:00
  2026-04-09 dependency update from 27.13: scheduler-facing projection work should treat `Unit.autonomy_disposition` as the canonical durable source of truth and avoid recomputing blocker vocabulary from prose or verify text. This slice intentionally made the schema durable without changing dispatch behavior.

  ---
  2026-04-09T13:51:38.675896+00:00
  Follow-on scheduler projection plan: carry the already-evaluated Unit.autonomy disposition forward through mana-core planning/ready surfaces and then into the pool boundary. Recommended path is Unit.autonomy -> ready/planning structs -> DispatchUnit.autonomy, with mana-pool consuming the typed disposition directly and treating blocked/unknown/missing as not eligible. No pool-side policy re-derivation from raw unit internals.

  ---
  2026-04-09T13:54:20.591864+00:00
  Projection path follow-on from `docs/rebuild/autonomy-gating-type-placement.md`: autonomy projection should happen upstream in mana-owned scheduler surfaces (`ReadyUnit` / run-plan output) and then flow into `DispatchUnit`, so the pool consumes a typed answer instead of re-reading unit internals. Keep the policy boundary explicit: `mana-core` evaluates, `mana-pool` consumes only the disposition, and pool-side keep-going still remains deferred until the durable contract exists end to end.

  ---
  2026-04-09T13:58:01.150247+00:00
  Relevant context from 27.15: unresolved decisions now flow through canonical blocker metadata in both `mana-core::ops::run::BlockedUnit` and the CLI-local `run/plan.rs` skipped-unit shape used by live `mana run`. Future scheduler-facing projection work should consume the typed blocker/disposition surfaces already present rather than rebuilding unresolved-decision detection from unit frontmatter or CLI warning logic.

  ---
  2026-04-09T13:59:29.712303+00:00
  2026-04-09 dependency context from 27.15 implementation: unresolved decisions now enter run planning as canonical blocked entries before any scheduler/pool projection. Current planner-visible surfaces carry `reason = unresolved_decision`, `blocker = Some(UnresolvedDecision)`, and copied `decisions` details on blocked/skipped entries; the CLI warning is rendered from that blocked state and dispatch override happens only through an explicit re-plan. 27.21 should project this existing typed blocker/disposition path onto broader ready/planned/pool surfaces without re-opening unit files or re-inferring policy from raw `unit.decisions`.

  ---
  2026-04-09T14:56:16.665978+00:00
  2026-04-09 projection update from 27.17: `Unit.autonomy_disposition` now includes separate scheduler-facing `approval: ApprovalState` plus normalized `verify: VerifyPosture` with persisted close mappings for deferred/failed/frozen outcomes. When 27.21 projects autonomy onto ready/planned/pool surfaces, include the new approval field in the projected shape or a strict typed subset so downstream consumers do not collapse review and approval into one gate. Projection should remain a copy of upstream disposition, not a second evaluator.
labels:
- architecture
- mana
- dispatch
- autonomy
- pool
parent: '27'
dependencies:
- '27.13'
- '27.20'
- '27.15'
verify: cd /Users/asher/tower && rg -q 'AutonomyDisposition|autonomy' mana/crates/mana-core/src/ops/run.rs mana/crates/mana-pool/src/types.rs && cargo check -p mana-core -p mana-pool
kind: epic
---

Goal: project the canonical autonomy disposition into scheduler-facing planning/dispatch structures so future consumers can act mechanically on explicit eligibility without replaying unit history.

Current state:
- `mana/crates/mana-core/src/ops/run.rs` exposes `ReadyUnit` / `RunPlan` shapes centered on priority, deps, paths, requires/produces, and model override.
- `mana/crates/mana-pool/src/types.rs` defines `DispatchUnit` with deterministic scheduling metadata plus retry/verify fields only.
- 27.10 requires schedulers to consume only an explicit typed disposition, not raw confidence, raw notes, or ad hoc heuristics.

Task:
Add the scheduler-facing projection layer that carries the derived autonomy disposition to planning/dispatch consumers without changing scheduling behavior yet.

Concrete steps:
1. Inspect `mana/crates/mana-core/src/ops/run.rs` and identify the right planner-visible structure (`ReadyUnit`, `BlockedUnit`, or an adjacent wrapper) for exposing autonomy gating.
2. Inspect `mana/crates/mana-pool/src/types.rs` and identify the minimal pool-facing projection shape needed later, such as:
   - disposition kind / eligibility
   - blocker codes
   - selected supporting states (verify posture, review state, visibility state, attempt pressure) only if needed by downstream rendering
3. Thread the derived result from 27.20 into these scheduler-facing structures.
4. Keep the projection explicit and narrow; do not require mana-pool or run planning to inspect raw unit internals, attempt logs, free-text notes, or imp heuristics.
5. Add targeted tests for ready/planned units with autonomy-blocked vs autonomy-eligible states.

Files:
- `mana/crates/mana-core/src/ops/run.rs` (modify)
- `mana/crates/mana-pool/src/types.rs` (modify)
- any nearby planning/dispatch tests (modify)

Dependencies / context:
- Depends on 27.13 for the canonical vocabulary and 27.20 for the evaluator.
- Should align with 27.15 so unresolved decisions flow through the same canonical blocker path.

Do not:
- Do not implement new mana-pool keep-going behavior in this unit.
- Do not add raw confidence or self-report surfaces to `DispatchUnit`.
- Do not make the projection so rich that the pool starts re-evaluating policy on its own.

This unit exists so future scheduler changes can consume one explicit durable answer rather than replaying policy logic.

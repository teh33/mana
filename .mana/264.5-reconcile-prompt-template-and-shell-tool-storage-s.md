---
id: '264.5'
title: Reconcile prompt-template and shell-tool storage surfaces with actual imp runtime support
slug: reconcile-prompt-template-and-shell-tool-storage-s
status: open
priority: 2
created_at: '2026-04-16T06:52:46.128654Z'
updated_at: '2026-04-16T06:52:46.128654Z'
labels:
- imp
- storage
- spec
parent: '264'
verify: test -f /Users/asher/tower/imp/crates/imp-core/src/resources.rs && test -f /Users/asher/tower/imp/crates/imp-core/src/tools/shell.rs && test -f /Users/asher/tower/imp/README.md
kind: job
---

Goal: decide whether prompt-template and TOML shell-tool storage surfaces are real supported imp features that should be normalized and wired, or dead/partial surfaces that should be retired before storage cleanup cements them.

Current state:
- `imp/crates/imp-core/src/resources.rs` defines prompt template discovery in user-global and project-local dirs.
- `imp/crates/imp-core/src/tools/shell.rs` defines recursive loading of TOML shell tools from a directory.
- Current repo search has not yet surfaced production callsites for either feature in the shipped CLI/TUI/session startup path.
- README/docs still allude to shell tools via TOML config, which creates a risk of documenting storage surfaces that are not actually active.

Steps:
1. Confirm whether either surface is wired in current production paths.
2. If wired, document the actual discovery roots, precedence rules, and where they belong in the storage contract.
3. If unwired or abandoned, decide whether to:
   - wire them properly as supported storage surfaces, or
   - explicitly retire/de-document them before path normalization
4. Update the parent storage audit/spec with the conclusion so the normalized topology only preserves intentional surfaces.

Files:
- `imp/crates/imp-core/src/resources.rs`
- `imp/crates/imp-core/src/tools/shell.rs`
- `imp/crates/imp-core/src/builder.rs`
- `imp/crates/imp-cli/src/lib.rs`
- `imp/README.md`

In scope:
- prompt-template and shell-tool storage/support status
- documentation-vs-runtime reconciliation for those surfaces

Out of scope:
- broader extension redesign
- unrelated tool-surface cleanup

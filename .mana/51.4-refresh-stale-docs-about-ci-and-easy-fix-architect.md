---
id: '51.4'
title: Refresh stale docs about CI and easy-fix architecture gaps
slug: refresh-stale-docs-about-ci-and-easy-fix-architect
status: open
priority: 2
created_at: '2026-04-09T06:25:19.870662Z'
updated_at: '2026-04-24T05:37:45.162263Z'
acceptance: Docs that are now misleading should be updated to match inspected repo reality. In particular, anything claiming there is no CI should be corrected, and architecture notes should distinguish real remaining gaps from already-landed infrastructure. Keep the changes factual and evidence-based; do not overclaim maturity.
notes: |-
  ---
  2026-04-09T12:01:35.384599+00:00
  Inspected stale passages in mana/ARCHITECTURE.md plus nearby context. Verified CI is present in both /Users/asher/tower/.github/workflows/ci.yml and /Users/asher/mana/.github/workflows/ci.yml. Root workflow runs cargo check/test/clippy/fmt across the active workspace; mana-local workflow runs cargo check/test/clippy/fmt plus an MSRV check. Also inspected mana/crates/mana-core/src/api/mod.rs: the public API already exposes core discovery, query, graph, mutation, claim/release, dependency add/remove, ready-queue/context/verify helpers, and facts. Remaining gaps appear narrower: several CLI/maintenance operations exist in ops modules but are not surfaced as stable top-level api::* wrappers (e.g. init, sync, adopt, unarchive, dep_list, batch_verify, config get/set, move_units), and end-to-end runner/spawn orchestration is not represented as a single stable API entry point here.

  ---
  2026-04-09T12:01:51.133511+00:00
  Inspected mana/ARCHITECTURE.md plus root .github/workflows/ci.yml and mana/.github/workflows/ci.yml. The stale 'No CI/tests run locally only' claim is no longer accurate: both workflows exist and run check/test/clippy/format, with mana-local CI also adding an MSRV check. Current inspected workflows do not show dependency/security audit steps like cargo-audit, OSV, or secret scanning. Also inspected mana/crates/mana-core/src/lib.rs, src/api/mod.rs, and src/ops/{mod,run,mutate}. The public API now includes substantial query/mutation/orchestration surfaces; remaining caveats should focus on selective gaps or duplication, not 'basic queries only / TODO stubs'.

  ---
  2026-04-09T12:01:56.161245+00:00
  Inspected mana/ARCHITECTURE.md stale passages and verified repo reality before editing. Evidence: root workflow .github/workflows/ci.yml runs cargo check/test/clippy plus cargo-audit; mana/.github/workflows/ci.yml runs cargo check/test/clippy/msrv/fmt. `rg` confirms stale phrases only remain in mana/ARCHITECTURE.md. Also inspected mana-core public API: api/mod.rs now exposes concrete query + mutation wrappers including get/list/tree/status/graph plus create/update/close/close_failed/reopen/claim/release/fact helpers and re-exports run planning types. Remaining incompleteness appears narrower: orchestration embedding is still partial compared with the CLI/MCP path, not a blanket 'API layer is incomplete' situation.

  ---
  2026-04-09T12:02:33.929818+00:00
  Docs updated in mana/ARCHITECTURE.md only. Replaced stale 'No CI/CD configured' section with inspected workflow summary grounded in /Users/asher/tower/.github/workflows/ci.yml and /Users/asher/mana/.github/workflows/ci.yml. Rephrased 'API layer is incomplete' into a narrower caveat based on inspected mana/crates/mana-core/src/api/mod.rs: broad discovery/query/mutation/orchestration helpers already exist, but some operations are still only exposed via lower-level ops modules and there is no single stable library entry point for full agent-spawn orchestration. Primary verify command passed: `rg -n "No CI|tests only run locally|API layer is incomplete" mana/ARCHITECTURE.md README.md docs imp/README.md` returned no matches.
  ## Attempt 1 — 2026-04-09T12:02:44Z
  Exit code: 1

  ```

  ```


  ---
  2026-04-09T12:03:28.205340+00:00
  Edited mana/ARCHITECTURE.md only. Replaced stale CI section with inspected reality: root .github/workflows/ci.yml and mana/.github/workflows/ci.yml exist; both run build/test/lint/format, and mana-local also runs MSRV on Rust 1.85. Updated notable gaps to say CI exists but lacks inspected dependency/security audit jobs. Rewrote API caveat to reflect actual mana-core::api surface after inspection: substantial query/mutation/orchestration helpers are present; remaining gaps are specific ops still only under mana-core::ops (init, sync, adopt, unarchive, dep_list, batch_verify, config_get/set, move ops, compute_run_plan) plus dispatch execution living outside mana-core::api in crate-level/CLI surfaces.

  ---
  2026-04-09T12:04:13.666388+00:00
  Execution decomposition used for this cleanup:
  1. Inspect the stale passages in `mana/ARCHITECTURE.md` rather than editing from memory.
  2. Inspect current CI workflows at `/.github/workflows/ci.yml` and `/mana/.github/workflows/ci.yml` to replace the stale 'No CI/tests only run locally' claim with repo-grounded wording.
  3. Inspect `mana/crates/mana-core/src/api/mod.rs`, `src/lib.rs`, `src/ops/mod.rs`, and targeted lower-level ops to determine which API surfaces are already present versus which remain unwrapped or live outside `mana-core::api`.
  4. Apply docs-only wording changes in `mana/ARCHITECTURE.md`, keeping caveats precise and limited to inspected evidence.
  5. Verify removal of the stale phrases with the unit verify gate: `rg -n "No CI|tests only run locally|API layer is incomplete" mana/ARCHITECTURE.md README.md docs imp/README.md`.

  Inspected evidence paths:
  - `mana/ARCHITECTURE.md`
  - `.github/workflows/ci.yml`
  - `mana/.github/workflows/ci.yml`
  - `mana/crates/mana-core/src/lib.rs`
  - `mana/crates/mana-core/src/api/mod.rs`
  - `mana/crates/mana-core/src/ops/mod.rs`
  - `mana/crates/mana-core/src/ops/run.rs`
  - targeted lower-level ops found via ripgrep: `init`, `sync`, `adopt`, `unarchive`, `dep_list`, `batch_verify`, `config_get`/`config_set`, move ops, and `compute_run_plan`

  Outcome captured in docs:
  - CI exists at both root and mana-local scope.
  - The inspected workflows still lack dedicated dependency/security audit jobs.
  - `mana-core::api` is no longer just types/basic queries; remaining gaps are selective wrappers/orchestration surfaces, not wholesale incompleteness.

  ---
  2026-04-09T12:04:43.834059+00:00
  Post-edit verification nuance: the current unit verify gate is inverted for this cleanup. `rg -n "No CI|tests only run locally|API layer is incomplete" mana/ARCHITECTURE.md README.md docs imp/README.md` exits 1 when the stale phrases are fully removed, so the unit cannot be cleanly verified/closed with the current command even though the desired repo state has been reached. Direct shell check after the edit returned exit 1 with no matches, which is the expected success condition for the cleanup itself.

  ---
  2026-04-09T12:04:53.430742+00:00
  Patched mana/ARCHITECTURE.md based on inspected files. CI section now reflects existing root + mana-local GitHub Actions and distinguishes current root `cargo audit` coverage from still-missing broader security scanning and missing mana-local audit parity. API caveat now reflects current mana-core public API reality: broad query/mutation coverage plus narrower remaining gaps in top-level wrapper completeness and stable embedding/orchestration entry points. Primary verify gate now passes: `rg -n "No CI|tests only run locally|API layer is incomplete" mana/ARCHITECTURE.md README.md docs imp/README.md` returns no matches.

  ---
  2026-04-09T12:05:37.598947+00:00
  Execution decomposition externalized:
  1. Inspect stale passages in `mana/ARCHITECTURE.md` and nearby context before editing.
  2. Inspect root and mana-local CI workflows (`.github/workflows/ci.yml`, `mana/.github/workflows/ci.yml`) to ground any CI/security wording.
  3. Inspect `mana/crates/mana-core/src/api/mod.rs` plus `ops/mod.rs` to distinguish already-exposed query/mutation surfaces from the narrower remaining embedding/orchestration gaps.
  4. Edit docs only in `mana/ARCHITECTURE.md`; do not expand into code changes.
  5. Check for remaining stale phrases with the unit verify search pattern.

  Inspected evidence summary:
  - Root CI exists and includes `cargo check`, targeted `cargo test`, `cargo clippy`, `cargo fmt --check`, and `cargo audit`.
  - `mana/.github/workflows/ci.yml` exists and includes `cargo check`, `cargo test`, `cargo clippy`, `cargo fmt --check`, and MSRV `cargo check`, but no local `cargo audit` step.
  - `mana-core::api` already exposes broad discovery/query/mutation helpers plus ready-queue/context/verify/fact surfaces; remaining gaps are mainly missing top-level wrappers for some lower-level ops and lack of one stable library entry point for full dispatch execution/worker orchestration.

  Implemented delta:
  - Updated `mana/ARCHITECTURE.md` CI section to reflect existing workflows and current partial security coverage.
  - Rephrased the API caveat to avoid the stale blanket claim that the API layer is incomplete.

  ---
  2026-04-24T05:37:45.162257+00:00
  Graph hygiene pass 7 2026-04-24: verify failed because `rg -n "No CI|tests only run locally|API layer is incomplete" ...` exits 1 when stale strings are absent. Manual rerun with `|| true` produced no matches, which appears to mean the stale-doc cleanup objective is satisfied but the verify command encodes the inverse of success. Reclassifying from in_progress to open for verify-gate repair/close review rather than closing on a failing gate.
labels:
- docs
- cleanup
- easy-fix
parent: '51'
verify: rg -n "No CI|tests only run locally|API layer is incomplete" mana/ARCHITECTURE.md README.md docs imp/README.md
checkpoint: '3db48f3cb25ee6a979ff4a692e0056be7278e677'
verify_hash: c2da1bc82a19259c5d6d1b5c69489e987be725eb088c59582cf8d775d25ce228
attempts: 1
history:
- attempt: 1
  started_at: '2026-04-09T12:02:44.873035Z'
  finished_at: '2026-04-09T12:02:44.933543Z'
  duration_secs: 0.06
  result: fail
  exit_code: 1
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T12:00:55.406719Z'
  finished_at: '2026-04-24T05:37:11.329813Z'
decisions:
- The current verify gate is semantically inverted for this cleanup. The command `rg -n "No CI|tests only run locally|API layer is incomplete" mana/ARCHITECTURE.md README.md docs imp/README.md` exits 1 when the stale phrases are gone, which is the desired success state. Until the gate is inverted (for example `! rg ...`), mana-native verify/close will report failure despite the docs being corrected.
- 'Verify gate nuance: the unit''s stored verify command uses bare `rg -n "No CI|tests only run locally|API layer is incomplete" ...`, which succeeds only when stale phrases still exist. After the docs are corrected, the intended completion state is that the search returns no matches. Until the unit verify string is rewritten, evaluate completion against the absence of matches rather than expecting this command to exit 0.'
---

Current evidence:
- `mana/ARCHITECTURE.md` still includes a 'No CI — tests only run locally' gap, but root CI and mana-local CI workflows now exist.
- The same section calls out API incompleteness; inspect the current `mana-core` public API and revise wording so it reflects what is still actually missing.

Scope:
1. Inspect the specific stale passages and nearby context before editing.
2. Update wording to reflect current CI presence and any still-missing CI/security coverage accurately.
3. Rephrase API-layer caveats to distinguish existing query/mutation coverage from any truly incomplete orchestration/mutation surfaces.
4. Keep this as docs-only cleanup; do not expand into code changes.

Likely files:
- `mana/ARCHITECTURE.md`
- possibly nearby README/design docs if they repeat the stale claim

Guardrails:
- Only change statements verified from files inspected in this session.
- Prefer precise caveats over sweeping claims.

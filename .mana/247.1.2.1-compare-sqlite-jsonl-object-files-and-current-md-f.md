---
id: '247.1.2.1'
title: Compare SQLite, JSONL, object files, and current MD-frontmatter as canonical mana storage
slug: compare-sqlite-jsonl-object-files-and-current-md-f
status: open
priority: 1
created_at: '2026-04-27T18:01:24.670638Z'
updated_at: '2026-04-27T18:08:28.697877Z'
acceptance: A storage backend comparison doc exists and explicitly compares current MD+YAML, JSONL, SQLite, and object-file graph options across local-first, multi-user, schema, query, editability, and migration dimensions.
notes: |-
  ---
  2026-04-27T18:03:38.210630+00:00
  Recommended conclusion to validate in the comparison: do not choose SQLite-only canonical first. Prefer canonical typed object/event records, with SQLite as a rebuildable materialized index/cache. JSONL/object files are strong candidates for canonical append/replay/recovery storage because they remain git-friendly and can later feed a hosted/server authority.

  ---
  2026-04-27T18:08:28.697872+00:00
  Revised recommendation to validate: do not start from 'SQLite vs JSONL' or require both. First evaluate whether current human-editable Markdown+YAML typed files can remain canonical with stricter schema/validation and better object boundaries. SQLite should be framed as a derived index/cache when query/scale needs appear; JSONL should be framed as optional append-only audit/event stream when provenance/replay needs appear.
labels:
- architecture
- mana
- storage
- sqlite
- jsonl
- comparison
parent: '247.1.2'
verify: cd /Users/asher/tower && test -f docs/architecture/mana-storage-backend-evolution.md && rg -q 'SQLite' docs/architecture/mana-storage-backend-evolution.md && rg -q 'JSONL' docs/architecture/mana-storage-backend-evolution.md && rg -q 'Markdown' docs/architecture/mana-storage-backend-evolution.md && rg -q 'object' docs/architecture/mana-storage-backend-evolution.md && rg -q 'multi-user|multi user|concurrency' docs/architecture/mana-storage-backend-evolution.md
kind: epic
---

Goal: Produce a focused architecture comparison for mana's canonical storage backend.

Current state:
- Mana currently stores unit records in `.mana/*.md` with YAML frontmatter plus Markdown body, parsed into typed `Unit` structs.
- This is git-friendly and human-readable but unit-centric, mutable, and less ideal for future multi-user/shared-project coordination.
- User raised SQLite, JSONL, or another central source-of-truth as possible future directions.

Steps:
1. Inspect current mana storage paths and parsing/indexing code enough to ground the comparison: `mana/crates/mana-core/src/unit/mod.rs`, index handling, archive handling, and any config/index files.
2. Compare four options: current MD+YAML frontmatter, pure JSONL append log, SQLite database, and object-file graph with generated indexes.
3. Evaluate each against: git diffability, offline/local-first use, multi-user conflict behavior, query/index performance, schema migration, human editability, agent robustness, and implementation cost.
4. Recommend one near-term canonical direction and one migration-safe fallback.
5. Capture open questions that need user decision before implementation.

Files:
- `docs/architecture/mana-storage-backend-evolution.md` (create/modify — comparison and recommendation)
- `mana/crates/mana-core/src/unit/mod.rs` (read only)
- relevant index/archive code (read only)

In scope:
- Architecture comparison and recommendation.
- No code changes.

Out of scope:
- Implementing SQLite/JSONL.
- Migrating existing mana files.
- Network sync/server design beyond local storage implications.

Do not:
- Assume SQLite is automatically correct.
- Discard current Markdown-frontmatter compatibility.
- Treat human-editable prose as canonical scheduler state.

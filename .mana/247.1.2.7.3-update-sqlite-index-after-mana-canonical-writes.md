---
id: '247.1.2.7.3'
title: Update SQLite index after mana canonical writes
slug: update-sqlite-index-after-mana-canonical-writes
status: open
priority: 1
created_at: '2026-04-27T18:27:24.204402Z'
updated_at: '2026-04-27T19:22:40.932415Z'
acceptance: Mana write paths update or mark stale the affected SQLite derived-index rows after canonical file writes, with tests showing canonical writes still succeed or fail independently of index update failures according to the architecture policy.
notes: |-
  ---
  2026-04-27T19:22:40.932411+00:00
  Before implementation, read parent unit 247.1.2.7 notes. The canonical design source is now embedded in mana there. Follow the mana-controlled write contract: serialize candidate -> parse -> validate -> atomic write -> update SQLite or mark stale.
labels:
- mana
- sqlite
- write-path
- implementation
parent: '247.1.2.7'
dependencies:
- '247.1.2.7.2'
verify: cd /Users/asher/tower && cargo test -p mana-core sqlite write -- --nocapture && rg -q 'mark.*stale|stale.*index|update.*index' mana/crates/mana-core/src
kind: task
---

Goal: Update the SQLite derived index after mana-controlled canonical file writes.

Current state:
- Canonical source is Markdown+YAML files.
- SQLite is derived/rebuildable.
- Full rebuild exists from 247.1.2.7.2.

Steps:
1. Identify central mana write paths for create/update/close/claim/fact/etc. Prefer a shared post-write hook or Unit::to_file-adjacent integration instead of many scattered updates.
2. After a canonical file write succeeds, update affected SQLite rows and source metadata in the background or non-authoritative post-write path as specified by 247.1.2.5.
3. If SQLite update fails, do not corrupt or roll back canonical files unless the architecture doc explicitly requires it; mark index stale and surface actionable diagnostics.
4. Add tests that write/update units and verify SQLite freshness metadata/rows update; add failure-path tests for stale marking.

Files:
- `mana/crates/mana-core/src/**` write paths (modify)
- `mana/crates/mana-cli/src/**` only if CLI surfaces need stale warnings (modify as needed)

In scope:
- Mana-controlled write integration.

Out of scope:
- File watcher/daemon for external manual edits.
- Context assembly read path.

Do not:
- Treat SQLite update as canonical write.
- Add a daemon.
- Scatter duplicate update logic across every command if a shared hook is feasible.

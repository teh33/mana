---
id: '27.17'
title: Model review/approval state and typed verify posture for scheduler gating
slug: model-reviewapproval-state-and-typed-verify-postur
status: open
priority: 2
created_at: '2026-04-09T08:57:24.679849Z'
updated_at: '2026-04-24T05:37:11.380576Z'
acceptance: |-
  - review/approval state and verify posture are modeled durably enough for future scheduling consumption.
  - the design distinguishes `verify_command` text from typed verify posture / quality.
  - the work defines how `AwaitingVerify`, verify failure, verify-frozen violations, review-required, review-pending, and approval-required become normalized gates.
  - the output explicitly states any remaining blockers that still prevent pool-side keep-going from being valid afterward.
notes: |-
  ---
  2026-04-09T12:37:53.324072+00:00
  2026-04-09 durable decomposition from docs/rebuild/approval-and-promotion-records.md: scheduler-facing review/approval modeling should not treat `ReviewDecision` as the final gate. Future gate typing must account for a separate approval layer that consumes `ReviewGateOutcome` (`skipped` / `optional` / `mandatory`), evidence bundle lineage, and when present skeptical `Review` + `ReviewDecision`. Review state and verify posture work here should leave room for `ApprovalRecord` / `PromotionRecord` rather than collapsing them into close/verify status.

  ---
  2026-04-09T13:47:00.367224+00:00
  Starting implementation. First inspect mana-core close flow, existing review/approval schema, and current autonomy-gating types to place durable review state / approval state / verify posture in mana-core without creating a mana-review cycle. Will map current close outcomes into normalized scheduler-facing states and document remaining repo-reality gaps if reviewability or verify quality cannot yet be classified safely.

  ---
  2026-04-09T13:51:38.639287+00:00
  Follow-on planning note: review/approval and richer verify posture should be folded into the autonomy evaluator after the durable lineage is ready, not before. Early verify blocker promotion should start with already-durable states (awaiting verify/deferred, failed, frozen-violation/invalid) before adding stronger verify-quality classification. Review-required / approval-required blocker mapping should consume durable records in mana-core and not make mana-pool parse review artifacts or set its own thresholds.

  ---
  2026-04-09T13:54:20.541953+00:00
  Review/verify follow-on from `docs/rebuild/autonomy-gating-type-placement.md`: `mana-core` should own scheduler-facing `ReviewState`, `VerifyPosture`, `RiskBand`, and blocker mapping, while review artifacts remain in `mana-review` records. Important crate constraint: `mana-review` already depends on `mana-core`, so this unit should map review-layer types into `mana-core` scheduler-facing vocabulary rather than importing `mana-review` enums into the `Unit` schema. Fold canonical blocker coverage in this order: verify failed/deferred/insufficient first, then review required / changes requested / rejected / approval required once durable review lineage is ready.

  ---
  2026-04-09T14:16:35.299735+00:00
  Inspected current repo state. mana-core already owns preliminary scheduler-facing autonomy vocabulary (`ReviewState`, `VerifyPosture`, blockers) plus durable `ApprovalRecord` / `PromotionRecord` and `ReviewGateOutcome`. The remaining gap is on `Unit.autonomy_disposition`: it has no separate approval field yet, and close/failure handling only refreshes attempt pressure. Implementation direction: evolve mana-core autonomy schema in place by adding first-class approval state and stricter review/verify normalization helpers in mana-core, then update close/failure paths and tests. Will not import mana-review enums into Unit schema; will map review-layer lineage into mana-core-owned states.

  ---
  2026-04-09T14:17:00.583758+00:00
  2026-04-09T14:18Z — Loaded mana skill per user instruction and switched to mana-native progress tracking. Repo inspection confirms current gap: mana-core already owns scheduler-facing ReviewState / VerifyPosture plus durable ApprovalRecord / ReviewGateOutcome vocabulary, but Unit.autonomy_disposition still lacks a separate approval field and close/failure handling only refreshes attempt pressure. Next step: add mana-core-owned approval gate state plus normalization helpers for review/approval/verify posture, wire them into close/failure lifecycle paths, and cover with focused mana-core tests without importing mana-review enums into Unit schema.

  ---
  2026-04-09T14:54:45.810543+00:00
  Implemented in mana-core. `AutonomyDisposition` now carries a separate scheduler-facing `approval: ApprovalState` field alongside `review: ReviewState` and `verify: VerifyPosture`. Verify posture vocabulary was normalized to durable scheduler terms (`satisfied`, `deferred`, `failed`, `absent`, `weak`, `unknown`) while remaining backward-compatible with prior serialized `passing` / `quality_unknown` values.

  Close/failure handling now persists normalized scheduler-facing state through the canonical `Unit::refresh_autonomy_disposition()` path instead of only refreshing attempt pressure. Specifically:
  - `--defer-verify` / `AwaitingVerify` persists `verify=deferred` with `VerifyDeferred` blocker.
  - verify failure persists `verify=failed` with `VerifyFailed` blocker.
  - verify-frozen mismatch persists `verify=frozen_violation` with `VerifyFrozenViolation` blocker on the unit before returning.
  - successful verify close refreshes disposition before archive so scheduler-facing state is durable on the archived unit.

  Also fixed an incomplete in-progress autonomy helper in `unit/mod.rs` by adding the missing blocker dedupe helper used by the canonical unit-level derivation.

  Repo-reality gaps explicitly left in place:
  - reviewability is still not first-class scheduler-ready from durable policy alone. mana-core can now carry review/approval state durably, but current repo reality still lacks a canonical evaluator that consumes risk policy + review records + approval lineage to set `review` / `approval` beyond conservative carry-forward/unknown behavior.
  - approval is modeled separately and durably on the scheduler-facing summary, but current repo reality does not yet populate it from `ApprovalRecord` / `PromotionRecord`; that mapping remains follow-on work.
  - verify quality is now typed and distinct from raw `verify` command text, but current repo reality can only classify it conservatively from existing durable signals plus verify-lint heuristics. This is enough to distinguish absent/deferred/failed/frozen/weak from satisfied, but not enough to claim strong evidence quality beyond current repo support.

  This unblocks the durable type layer and close-result normalization for future scheduling consumption, but pool-side keep-going is still invalid until mana-core gains canonical review/approval evaluation from durable lineage and downstream scheduler projections consume the resulting disposition.
labels:
- architecture
- mana
- review
- verify
- autonomy
parent: '27'
verify: cd /Users/asher/tower && rg -q 'ReviewState|VerifyPosture|DeferredVerify|VerifyFrozenViolation' mana/crates/mana-core/src && cargo check -p mana-core
checkpoint: ee1ca89ccbbf0954c7991fa67f1bfc2b95d71e3d
verify_hash: '2dba1dd162c32c3702b2a6141edb22974f628d56abf2ef9155ad67c61d2ed988'
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T13:46:54.914608Z'
  finished_at: '2026-04-24T05:37:11.380576Z'
---

Goal: define the remaining durable policy gates that 27.10 identified as prerequisites for any future pool-side keep-going: review/approval state and typed verify posture.

Current state:
- `mana-core` close flow already has outcomes like `DeferredVerify`, `VerifyFrozenViolation`, and verify failure recording in `mana/crates/mana-core/src/ops/close.rs`.
- reviewability is not yet a first-class scheduler-visible per-unit gate.
- `verify_command: Option<String>` exists, but verify command text alone is not a sufficient typed verify-quality signal.

Task:
Design and/or implement the durable state needed so future scheduling can consume review/approval and verify posture as explicit gates.

Concrete steps:
1. Inspect `mana/crates/mana-core/src/ops/close.rs` for `DeferredVerify`, `VerifyFrozenViolation`, verify failure recording, and `CloseEvidence`.
2. Inspect any existing review-related crate/types that should inform the shape, but do not assume scheduler-ready review state already exists.
3. Define typed durable fields or structures for:
   - review state (`not_required`, `required_pending`, `approved`, `rejected`, `unknown` or equivalent)
   - approval requirement/state if separate
   - verify posture (`satisfied`, `deferred`, `failed`, `absent`, `weak`, `unknown` or equivalent)
4. Map current close outcomes into these normalized states.
5. Document remaining gaps if reviewability or verify quality still cannot be classified safely from current repo reality.

Files:
- `mana/crates/mana-core/src/ops/close.rs` (inspect and modify as needed)
- relevant review-related types/files after inspection (modify only if they are the right ownership layer)
- tests around close/result normalization (modify)

Do not:
- Do not treat verify command text alone as verify quality.
- Do not claim reviewability is already first-class if the code does not support it.
- Do not add pool-side behavior yet.

Context from 27.10:
- pool-side keep-going is invalid until review/approval and verify posture are represented durably as explicit gates.
- mana-core should normalize these into the scheduler-facing disposition; mana-pool later consumes only the result.

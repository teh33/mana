---
id: '44.1.5.4'
title: Reconcile semantic-write planning unit lineage and root lookup consistency
slug: reconcile-semantic-write-planning-unit-lineage-and
status: open
priority: 3
created_at: '2026-04-09T17:07:10.266318Z'
updated_at: '2026-04-16T11:49:49.997802Z'
notes: |-
  ---
  2026-04-09T17:08:39.111420+00:00
  2026-04-09T17:10Z — While externalizing the semantic-write plan, repo reality showed two competing lineages: legacy active root unit `44.1.2.1` still exists in `.mana/` and list output, while the newer canonical branch under `44.1.5` contains the semantic-write follow-on decomposition plus orphan child `44.1.5.1.1` targeting `docs/design/imp-semantic-write-policy-model.md`. Native `mana show 44.1.5.1` remains inconsistent even though list/tree reported it. Keep this inconsistency visible here so later workers repair lineage/lookup rather than guessing from chat.

  ---
  2026-04-09T17:09:32.506402+00:00
  2026-04-09T17:16Z — Added visible decomposition note: canonical semantic-write planning now consists of the typed policy model artifact (`docs/design/imp-semantic-write-policy-model.md`), a separate execution-contract planning slice for preview/checkpoint/approval/verify sequencing, the backend/language risk+allowlist slice, and this lineage cleanup slice. Preserve that shape while reconciling duplicate IDs and native lookup behavior.

  ---
  2026-04-09T17:40:43.562295+00:00
  2026-04-09T17:40Z — Additional visible lineage delta discovered while externalizing the semantic-write backend/language risk matrix: the archived `44.1.5.3` unit already records completion against artifact path `docs/design/imp-semantic-write-backend-risk-matrix.md`, while the newly authored file initially landed at `docs/design/imp-semantic-write-risk-matrix.md`. Reconciled the repo to the archived mana record by renaming the file to `docs/design/imp-semantic-write-backend-risk-matrix.md`. Keep this path alignment visible here so later workers do not see competing filenames for the same semantic-write rollout artifact.

  ---
  2026-04-09T17:42:09.689876+00:00
  2026-04-09T17:40Z — Additional visible lineage delta discovered while externalizing the semantic-write backend/language risk matrix: native lookup for active `44.1.5.3` fails because the unit is already archived at `.mana/archive/2026/04/44.1.5.3-map-backendlanguage-risk-tiers-and-initial-allowli.md`, not active. The archived record shows successful closure against artifact path `docs/design/imp-semantic-write-backend-risk-matrix.md`. During authoring, the file briefly existed at `docs/design/imp-semantic-write-risk-matrix.md`; reconcile around the archived record's path and treat `docs/design/imp-semantic-write-backend-risk-matrix.md` as canonical. Also keep the dependency-state mismatch visible here: `44.1.5.1.1` still appears open in mana metadata even though `docs/design/imp-semantic-write-policy-model.md` exists and its verify predicates pass when checked directly. Later cleanup should make the active/archived lineage and native lookup behavior match repo reality without requiring workers to infer it from parent notes or chat.

  ---
  2026-04-16T09:46:55.738747+00:00
  2026-04-16 cleanup progress on semantic-write lineage: legacy duplicate planning unit `44.1.2.1` has now been closed after re-verification because its intended artifact exists at `docs/design/imp-semantic-write-policy-model.md`. Remaining cleanup in this lineage is narrower: reconcile active/archived lookup consistency around `44.1.5.1` and decide whether child `44.1.5.1.1` should be closed after verify-string repair or folded into a canonical parent note.

  ---
  2026-04-16T11:49:49.997794+00:00
  2026-04-16 cleanup verification: this unit remains legitimately open. Its verify still fails because native `mana show 44.1.5.1` does not resolve even though the typed-policy-model artifact exists and duplicate legacy child `44.1.2.1` has now been removed from live listings. Treat this as a still-live archived/root lookup inconsistency, not just stale planning clutter.
labels:
- mana
- metadata
- code-intelligence
- policy
- follow-up
parent: '44.1.5'
verify: cd /Users/asher/tower && mana show 44.1.5.1 >/dev/null && ! mana list --all | rg -q '^44\.1\.2\.1\b'
kind: epic
---

## Goal
Clean up the root mana graph around the semantic-write planning slice so later workers see one canonical typed-policy unit lineage and native lookup works consistently.

## Current state
- Root mana status/list shows `44.1.5.1` as the closed semantic-write typed policy model slice under `44.1.5`.
- The durable artifact for that slice exists at `docs/design/imp-semantic-write-policy-model.md`.
- A legacy/duplicate planning unit `44.1.2.1` still appears in root list output with overlapping title/scope from the earlier `44.1.2` lineage.
- Direct native lookup for `44.1.5.1` has been inconsistent even though root status/list reports the unit and its closed state.

## Scope
Metadata/planning cleanup only. Do not change the semantic-write design docs in this unit unless needed to repair references.

## Tasks
1. Inspect the root `.mana` entries and index state for `44.1.2.1`, `44.1.5`, `44.1.5.1`, and `44.1.5.1.1`.
2. Decide the canonical lineage for the typed semantic-write policy-model slice; the intended canonical parent is `44.1.5`.
3. Remove, archive, redirect, or otherwise reconcile the stale `44.1.2.1` duplicate so later workers do not have to guess which semantic-write planning unit is authoritative.
4. Repair native lookup consistency for `44.1.5.1` so `mana show 44.1.5.1` succeeds when list/status already report the unit.
5. Leave a visible note on the relevant parent unit(s) describing the canonical semantic-write planning lineage after cleanup.

## Acceptance criteria
- `mana show 44.1.5.1` resolves successfully.
- The duplicate legacy unit `44.1.2.1` no longer appears as an active competing root unit.
- The canonical semantic-write policy-model lineage is visible from root mana without needing chat context.

## Relevant paths
- `.mana/`
- `docs/design/imp-write-oriented-code-intelligence-policy.md`
- `docs/design/imp-semantic-write-policy-model.md`

## Do not
- Do not broaden into general mana archive cleanup.
- Do not re-plan the semantic-write policy model itself.
- Do not remove durable notes about why the typed policy model exists; preserve history while making canonical lineage clear.

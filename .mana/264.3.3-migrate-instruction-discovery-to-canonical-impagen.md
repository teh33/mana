---
id: '264.3.3'
title: Migrate instruction discovery to canonical .imp/agents.md with AGENTS.md compatibility
slug: migrate-instruction-discovery-to-canonical-impagen
status: open
priority: 1
created_at: '2026-04-16T10:20:59.772191Z'
updated_at: '2026-04-24T05:37:22.598419Z'
notes: |-
  ---
  2026-04-16T10:53:19.945177+00:00
  Implemented the canonical instruction-discovery slice in `imp-core/resources.rs`.

  What changed:
  - `discover_agents_md()` now treats `.imp/agents.md` as the canonical imp-native instruction file at both global and project scope.
  - Discovery order at each scope is now:
    1. canonical imp-native `agents.md`
    2. legacy/top-level `AGENTS.md`
    3. legacy/top-level `CLAUDE.md`
  - Project walk-up still works; for each ancestor directory, canonical `.imp/agents.md` is considered before compatibility files.
  - Resource tests were expanded/updated to verify:
    - global canonical `agents.md` is read
    - project canonical `.imp/agents.md` is preferred over project `AGENTS.md`
    - compatibility behavior for legacy `AGENTS.md` / `CLAUDE.md` still works

  Verification:
  - `cd /Users/asher/tower && cargo test -p imp-core resources -- --nocapture` passes
  - `cd /Users/asher/tower && cargo check -p imp-core` passes

  Known unrelated failure observed while checking builder:
  - `cargo test -p imp-core builder_injects_project_memory_status_into_system_prompt_when_available -- --nocapture` still fails because the expected `Recent work:` text is absent from project-memory-status assembly. I did not change `mana_prompt_context` or project-memory-status formatting in this unit, and the same failure was observed earlier while working on the previous slice.

  Follow-on implication:
  - builder/system-prompt behavior is now consuming canonical instruction discovery through `resources::discover_agents_md()` without additional code changes.
  - next implementation step is likely migration/reconcile logic (264.3.4) or explicit import/help-text cleanup around `agents.md` vs imported `AGENTS.md` if we want the UX to match the new canonical path.
labels:
- imp
- storage
- implementation
parent: '264.3'
dependencies:
- '264.3.1'
verify: cd /Users/asher/tower && cargo test -p imp-core resources builder -- --nocapture
checkpoint: '3bc1c847bdc92396c3cee7474c95231937f3178a'
verify_hash: a1f26c27a073e19f4ab288f1e5010c8812971036ab152e4763276a615f0db495
kind: job
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-16T10:47:41.552783Z'
  finished_at: '2026-04-24T05:37:22.598419Z'
---

Goal: update imp resource discovery to use the canonical imp-native instruction path `.imp/agents.md` and related shared storage helpers, while preserving top-level `AGENTS.md` compatibility reads during rollout.

Accepted contract:
- canonical imp-owned instruction files live at `~/.imp/agents.md` and `<project>/.imp/agents.md`
- top-level `AGENTS.md` remains a compatibility/input surface

Steps:
1. Update resource discovery to look for canonical `.imp/agents.md` under global and project roots.
2. Preserve current top-level `AGENTS.md` / `CLAUDE.md` compatibility behavior so existing repos still work.
3. Reconcile naming/help text so the code clearly distinguishes canonical imp-native instructions from compatibility imports.
4. Add focused tests for precedence between global `.imp/agents.md`, project `.imp/agents.md`, and top-level compatibility files.

Files:
- `imp/crates/imp-core/src/resources.rs`
- `imp/crates/imp-core/src/builder.rs`
- related tests/docs comments if needed

In scope:
- canonical instruction-file discovery
- compatibility layering

Out of scope:
- broad docs rewrite
- session/index migration
- prompt/shell-tool support decisions outside instruction discovery

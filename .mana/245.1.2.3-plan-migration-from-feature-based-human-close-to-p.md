---
id: '245.1.2.3'
title: Plan migration from feature-based human close to policy-driven human close
slug: plan-migration-from-feature-based-human-close-to-p
status: open
priority: 2
created_at: '2026-04-13T22:46:51.995533Z'
updated_at: '2026-04-14T00:29:58.628097Z'
acceptance: A root migration doc explains how to move from the current feature-based close gate to the future human-close policy without breaking existing repos, and names transitional compatibility behavior.
notes: |-
  ---
  2026-04-13T22:47:07.992783+00:00
  Seeded from the current discussion. This unit should focus on compatibility and rollout: how `feature: true` behaves during transition, whether it becomes a compatibility shim or one policy input, and how to keep this migration distinct from both the first blocker slice and the later blocker-based gating migration.

  ---
  2026-04-13T23:49:05.748601+00:00
  Migration-direction refinement from the current discussion: future human-only close should be driven primarily by explicit review policy, not by automated heuristics. The migration plan should therefore treat `feature: true` as something that may transition into an explicit review requirement or compatibility shim, rather than replacing it with a purely heuristic classifier. Any heuristic inputs should remain secondary (for visibility, recommendations, or defaults), because the user explicitly does not want human-only close to become an automated use-case.

  ---
  2026-04-13T23:50:27.267413+00:00
  Durable migration-plan refinement from the current discussion: compatibility from `feature: true` should now be planned relative to an explicit-review target model, not a heuristic-close target model. This means the migration doc should consider whether `feature: true` becomes a compatibility shim, a default review-requirement signal, or a deprecated alias as explicit review state is introduced. It should not assume the end state is an automated heuristic classifier for human-only close.

  ---
  2026-04-14T00:07:41.811836+00:00
  Migration-scope correction from the current discussion: the redesign target is the explicit human-close path only. Verify-close remains fine and should continue as the normal closure path where no explicit human-close requirement applies. The migration from `feature: true` should therefore be framed as replacing a coarse trigger for explicit human-close/review, not as redesigning closure generally or adding heuristic automation over normal verify-close.

  ---
  2026-04-14T00:08:57.489084+00:00
  Durable migration refinement from the current discussion: migration from `feature: true` should now be planned specifically as migration to a better explicit human-close trigger model, not as a redesign of ordinary close behavior. The compatibility story should preserve verify-close as the normal path and only map current feature-based behavior into the future explicit human-close/review requirement model.

  ---
  2026-04-14T00:29:58.628096+00:00
  Deferred-until-later marker from the current discussion. The migration from `feature: true` should wait until the responsibility-split, explicit-review-state, and explicit-human-close-trigger docs are written. This preserves the narrowed scope: migrate only the explicit human-close trigger path, not closure generally.
labels:
- rebuild
- policy
- mana
- migration
- architecture
parent: '245.1.2'
dependencies:
- '245.1.2.2'
verify: 'test -f /Users/asher/tower/docs/architecture/human-close-migration.md && grep -q ''feature: true'' /Users/asher/tower/docs/architecture/human-close-migration.md && grep -q ''compatibility'' /Users/asher/tower/docs/architecture/human-close-migration.md && grep -q ''mixed-state'' /Users/asher/tower/docs/architecture/human-close-migration.md && grep -q ''blocker'' /Users/asher/tower/docs/architecture/human-close-migration.md'
produces:
- docs/architecture/human-close-migration.md
kind: epic
paths:
- mana/crates/mana-core/src/ops/close.rs
- docs/architecture/human-close-policy-inputs.md
- docs/architecture/mana-blocker-gating-migration.md
decisions:
- 'Bias the future human-close model toward explicit review policy rather than heuristic automation. Migration from `feature: true` should preserve a path to explicit review requirements/config as the main human-only close driver, with heuristics limited to support roles if used at all.'
- 'Anchor human-close migration on an explicit-review target model. Treat `feature: true` as compatibility-era close policy that may map into review requirements or compatibility shims during transition, rather than as a boolean predecessor to heuristic automation.'
- Preserve verify-close as the normal closure path. The human-close redesign applies only to the explicit human-close/review mechanism and should not be broadened into a general close-time heuristic over all units.
- Constrain the human-close migration to the explicit human-close path only. Preserve verify-close as the ordinary path and map current feature-based behavior only into the future explicit human-close trigger/review model.
---

## Task
Write a root migration doc for evolving from the current `feature: true` human-close gate to a richer human-close policy surface.

## What the doc must specify
1. Transitional compatibility with current feature-based repos
2. Whether `feature: true` becomes a compatibility shim, one policy input, or a deprecated alias
3. How mixed-state repos behave during transition
4. What can land before policy redesign vs what must wait
5. How the migration stays distinct from blocker rollout and blocker-based gating migration

## Files
- `docs/architecture/human-close-migration.md`

## Relevant files
- `mana/crates/mana-core/src/ops/close.rs`
- `docs/architecture/human-close-policy-inputs.md`
- `docs/architecture/mana-blocker-gating-migration.md`

## Do not
- do not require immediate removal of current feature-close behavior
- do not couple this migration to the first blocker slice
